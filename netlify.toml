[build]
  publish = "public"
  command = "cd node_modules/gatsby-source-strapi && yarn && yarn build && cd ../../ && gatsby build"
[build.environment]
  YARN_VERSION = "1.9.2"
  YARN_FLAGS = "--no-ignore-optional"
